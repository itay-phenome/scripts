// GENERATED by PhenomeOne UI Discovery - do not edit by hand.
// Source: output\ui-map.json
// Generated: 2026-08-24 22:52:31

import { test, expect } from '@playwright/test';
import { goToState } from './navigation';

// One smoke test per discovered UI state. Each asserts the controls the
// discovery tool validated are present, so a UI regression fails here first.

test.describe('PhenomeOne UI map smoke', () => {

  test('home exposes its controls', async ({ page }) => {
    await goToState(page, 'home');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('home-new-group')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Refresh', exact: true })).toBeVisible();
  });

  test('research-group-admin exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-admin');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-cultivars exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-cultivars');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-entities exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-entities');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-germplasms exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-germplasms');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-germplasms-dialog-add-germplasm exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-germplasms-dialog-add-germplasm');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-images exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-images');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-inventory exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-inventory');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-observations exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-observations');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-overview exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-overview');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-group-variables exposes its controls', async ({ page }) => {
    await goToState(page, 'research-group-variables');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('tablist', { name: 'Research group sections', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Overview', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Germplasms', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Variables', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Observations', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Cultivars', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Images', exact: true })).toBeVisible();
    await expect(page.getByRole('tab', { name: 'Inventory', exact: true })).toBeVisible();
  });

  test('research-groups exposes its controls', async ({ page }) => {
    await goToState(page, 'research-groups');
    await expect(page.getByRole('link', { name: 'Home', exact: true })).toBeVisible();
    await expect(page.getByTestId('nav-research-groups')).toBeVisible();
    await expect(page.getByRole('button', { name: 'Actions', exact: true })).toBeVisible();
    await expect(page.getByRole('heading', { name: 'Research Groups', exact: true })).toBeVisible();
    await expect(page.getByRole('searchbox', { name: 'Search groups', exact: true })).toBeVisible();
    await expect(page.getByRole('button', { name: 'Refresh', exact: true })).toBeVisible();
    await expect(page.getByRole('table', { name: 'Research Groups', exact: true })).toBeVisible();
    await expect(page.getByRole('link', { name: 'Research Group ABC', exact: true })).toBeVisible();
    await expect(page.getByRole('link', { name: 'Research Group XYZ', exact: true })).toBeVisible();
  });
});
